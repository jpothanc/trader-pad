export type OrderSummaryInfo = {
  orderValue: number;
  execValue: number;
  pendingValue: number;
  size: number;
  executed: number;
  pending: number;
  avgPrice: number;
  marketPercent: string;
};
